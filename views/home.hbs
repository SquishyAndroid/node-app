<!DOCTYPE html>
<html>
  {{> header}}
  <body>
  {{> navbar}}

  <div class="container">

    <div class="jumbotron">
      <h1>{{pageTitle}}</h1>
      <p>{{screamIt welcomeMessage}}</p>
    </div>
    
  </div>

  {{> footer}}
  {{> scripts}}
  </body>
</html>
