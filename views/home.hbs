<!DOCTYPE html>
<html>
  {{> header}}
  <body>
  {{> navbar}}
  <div class="container">
    <h1>{{pageTitle}}</h1>

    <p>{{screamIt welcomeMessage}}</p>
  </div>

    {{> footer}}
    {{> scripts}}
  </body>
</html>
