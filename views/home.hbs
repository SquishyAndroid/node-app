<!DOCTYPE html>
<html>
  {{> header}}
  <body>
  <div class="container">
    <h1>{{pageTitle}}</h1>
    {{> navbar}}

    <p>{{screamIt welcomeMessage}}</p>
  </div>

    {{> footer}}
  </body>
</html>
